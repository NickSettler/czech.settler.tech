import Vue from 'vue';
import Vuex, { StoreOptions } from 'vuex';
import Api from '@/classes/api';

Vue.use(Vuex);

export default new Vuex.Store({
    state: {},
    mutations: {},
    actions: {},
    modules: {},
});
